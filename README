LZJB, bit-compatible, but reinterpreted in pure functional Haskell, using only lists as data structures (to potentially use via Fay).

LZJB compression rates are uncompetitive with (for example) gzip.


